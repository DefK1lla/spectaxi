# Пользовательские сценарии (MVP)

## 1) Цель документа

Пользовательские сценарии SpexTaxi. Шаги взяты из продуктового плана и уточнений. Не описанные ветки не добавляются.

Поток заказа одинаковый для частного и регулярного бизнес-клиента. Плановый заказ входит в MVP.

## 2) Кто действует

Один пользователь — один аккаунт. В этом аккаунте можно быть:
- клиентом (частным или регулярным бизнес-клиентом; отдельной сущности компании в MVP нет);
- исполнителем (владелец/водитель спецтехники).

После входа по телефону — форма регистрации: имя и **режим** (`клиент` или `исполнитель`). Это сразу `current_mode`. Других полей роли (`default_mode`, флаги «включён клиент / исполнитель») нет. Интерфейс (меню, заказы, поиск / парк) зависит от `current_mode`. Переключение режима остаётся; при первом переключении на исполнителя создаётся `executor_profile`.

Контакты заполняют оба. На форме **всегда** видны: телефон для связи, Telegram, WhatsApp, MAX — по отдельности не обязательны. Можно добавить ещё мессенджеры (название + ссылка). Для создания и принятия заказа нужны **имя** и **хотя бы один** канал связи (телефон или любой мессенджер). Иначе предупреждение и ссылка в профиль.

**В поиске и до первого «принять» контакты не показываются.**

Свою технику запросить нельзя.

Внутренние рейтинги (`RATING_PLAN.md`) в UI **не показываются**. После завершённого заказа стороны оставляют друг другу **публичные отзывы** (звёзды и необязательный текст). Причины — `CANCELLATION_REASONS.md`. Правила сервиса и блокировка — `SERVICE_RULES.md`. Поиск — `SEARCH_ALGORITHM.md`.

У клиента и исполнителя есть экран всех уведомлений. У исполнителя — список входящих (не только пуш).

## 3) Вход и регистрация

1. Указать номер телефона.
2. Получить код подтверждения.
3. Ввести код.
4. Если новый пользователь — имя и режим (`клиент` / `исполнитель`) → `current_mode`.

## 4) Клиент: фильтр, поиск и запрос

Срочный и плановый поток один по кнопкам, отличается только датой. Алгоритм выдачи — `SEARCH_ALGORITHM.md`.

Сначала **фильтр заказа** (его же можно сохранить как черновик):
- категория транспорта (из БД);
- нужный тариф из разрешённых категории;
- количество (часы / смены / сутки / месяцы; для рейса — число рейсов, для км — километры);
- режим: срочный или плановый;
- для планового — **дата начала** (без даты и времени окончания);
- для срочного — **без** начала и окончания;
- точка работ;
- максимальная цена — **необязательно**.

Система считает стоимость по каждой машине: `ставка тарифа × количество`. Сумму никто не вписывает вручную. Длительность для занятости сервер выводит из тарифа и количества.

1. Поиск: карта и список по фильтру. Сортировка списка — ближайший первый, при равной дистанции — дешевле. Занятые на это время машины не показываются. Клиент сужает выдачу фильтрами: расстояние и потолок цены.
2. Карточка — единица транспорта: фото, локация, аватар и имя, посчитанная цена. Контактов нет.
3. Карта: точка машины, совпадение координат — `+N` и список снизу.
4. Экран машины и исполнителя, контакты скрыты.
5. Отправить запрос **этой** машине / исполнителю. Нельзя свою единицу. Нельзя машину без подтверждённой лицензии категории (её нет в выдаче).
6. Ждать ответ. На плановый заказ те же **5 минут**.

Запрос можно не отправлять, а **сохранить как черновик**. Черновики — отдельная вкладка на экране заказов.

Пока исполнитель не принял и не отказал, клиент может снять запрос и выбрать другого. После снятия этого же можно запросить снова. После отказа — скрыт по заказу, повторно нельзя. Не ответил за 5 минут — это отказ: скрыт, повторно нельзя. Одновременно ждёт ответа только один запрос.

Если исполнитель принял **другой** заказ на это же время — клиенту уведомление, **этот** заказ возвращается к поиску исполнителя (не новый заказ).

Пока заказ не в `in_progress`, любой срыв матча возвращает его к поиску исполнителя. После начала работы поиск на **этом** заказе не продолжается. Нужен новый заказ («такой же»).

## 5) Исполнитель: настройки, парк, лицензия, подписка

### 5.1. Статус работы

Переключатель: принимает / не принимает. Видимость в поиске ещё зависит от подписки (если флаг включён) и лицензии категории машины.

### 5.2. Подписка

Флаг `executor_subscription_enabled`. В MVP флаг **выключен**: активация исполнителя без оплаты. Когда флаг включён — для приёма заказов нужна оплаченная подписка (автоактивация после оплаты, см. `PRODUCT_FOUNDATION.md`).

### 5.3. Добавление транспорта

Лимита нет. Категории — строки таблицы `machinery_categories`. У строки: системный `code` типа транспорта, категория прав `license_category` (например `B`; пусто — лицензия не нужна), текст для интерфейса. Отдельного JSON-конфига лицензий нет: конфиг — сама строка типа.

При добавлении:
- категория (селект из таблицы типов);
- фото **файлами**;
- локация (автокомплит, в том числе адреса других его машин);
- тарифы из списка, разрешённого категории.

Если категории нужна лицензия, а у исполнителя нет **подтверждённой админом** лицензии на **эту категорию** — машину сохранить можно, в поиске её нет. Загрузка документов лицензии — один комплект на категорию (на все машины этой категории). Админ в админке: pending / approved / rejected.

Тарифы принадлежат единице. Справочник — `TARIFF_TYPES.md`.

## 6) Исполнитель: первое «принять»

Входящие — список заказов и уведомления. Открытие заказа из уведомления **не** отдельный статус: смотреть можно, но «просмотрел» никуда не пишется.

1. Заказ без контактов заказчика. «Принять запрос» / «Отклонить». 5 минут, в том числе на плановый.
2. «Принять запрос» — контакты открываются, статус `in_negotiation`. Это **не** фиксация условий. Если на это же время по этой машине висели другие запросы — их клиентам уведомление «исполнитель принял другой заказ», те заказы возвращаются к поиску.
3. «Отклонить» — причина стадии, исполнитель скрыт по заказу.
4. Таймаут 5 минут — как отказ: исполнителю **−3**, скрыт по заказу, повторно нельзя.
5. Если контактов не хватает (нет ни телефона, ни мессенджера) или нет имени — принять нельзя, ссылка в профиль.

После «принять» экран **другой**: детали заказа, контакты заказчика, кнопка **«Подтвердить условия»** (не повтор «принять»). Срок подтверждения условий — **1 сутки**.

## 7) Переговоры и фиксация условий

Статус `in_negotiation`. У заказчика — правка деталей (тариф, количество, дата начала у планового, адрес, описание). После сохранения `details_version` растёт, исполнителю уведомление, его «подтвердить» сбрасывается, дедлайн подтверждения условий снова 1 сутки. Подтверждение со старой версией — ошибка.

У исполнителя — «Подтвердить условия» (текущая посчитанная сумма) или отклонить партнёра.

Не подтвердил за сутки — заказ `published`, исполнитель скрыт, исполнителю **−3**.

Отклонить партнёра могут оба: заказ `published`, исполнитель скрыт, клиент ищет другого **в этом заказе**. Спора нет.

После «Подтвердить условия»:
- `direct_payment` — `in_progress`, создаётся назначение;
- `secure_deal` — `awaiting_payment`: форма оплаты у клиента, у исполнителя «не оплачен». Срок **1 сутки**. Холд после оплаты. Успех → `in_progress`. Не оплатил → платёж отменяется, клиенту **−3**, заказ **возвращается к поиску** (не отменяется).

На `awaiting_payment` заказчик может писать исполнителю через открытые контакты (и наоборот). Правка условий на этом статусе: вернуть/отменить холд или незавершённый платёж, снова `in_negotiation` (версия++, исполнителю снова подтверждать).

## 8) Работа и завершение

`in_progress`, пока **обе** стороны не подтвердят исполнение либо не откроют спор. Отмена кнопкой **нельзя** — только спор.

У обеих кнопка «работа выполнена». Когда нажали оба — `completed`, выплата при сделке, можно оставить отзыв.

Если один подтвердил, второй молчит **1 сутки** — автоподтверждение молчащего, заказ `completed` (работа считается выполненной).

- Молчал **клиент** — клиенту **−3**, исполнителю **+5** за успех; клиенту **+3** за успех нет.
- Молчал **исполнитель** — исполнителю тоже **−3**, и всё равно закрытие **в его пользу** (работа по сути сделана); клиенту **+3**, исполнителю **+5** за успех.

Спор с `in_progress` и с `completed`.

Кнопка «такой же заказ» на `completed` — после того как обе стороны подтвердили (или автоподтверждение). На `cancelled` — после отмены. Если заказ вернулся к поиску, «такой же» не нужен.

## 9) История

Активные — без `cancelled` и без черновиков (черновики на своей вкладке). Архив: фильтр отменённых. Завершённые и `disputed` видны. Заказы в поиске (`published`) — в активных.

## 10) Отмена

Кнопкой до начала работы: `draft` (удалить/отменить черновик), `published` (без причины), `in_negotiation`, `awaiting_payment` (с причиной стадии). На `awaiting_payment` если платёж уже создан — его отменяют/возвращают, затем заказ `cancelled`.

Нельзя кнопкой: `in_progress` (только спор), `completed`, `disputed` (только админ).

Отклонение партнёра и прочие срывы матча заказ **не** отменяют — возврат к поиску.

После полной отмены — «искать снова?» (новый заказ).

## 11) Спор

Только `in_progress` и `completed`. Одна сущность спора на заказ, сообщения в отдельной таблице. Апелляция открывает **тот же** спор повторно, не второй тред. Лимит апелляций — 1.

Причина при открытии, при `other` текст. Фото — во вложении **сообщения**. Админ пишет и закрывает только из админки (`completed` / `cancelled` / `in_progress`).

Автоматически в `disputed`: провайдер вернул **уже существующий** холд по `expires_at` (платёж был `authorized`). Если клиент не платил — провайдер ничего не возвращает, срабатывает наш таймаут с `awaiting_payment` → поиск.

Пока холд не выплачен, админ может направить деньги (выплата / возврат). Если выплата уже ушла (`captured`), админ деньги **не контролирует** — только санкции к пользователям.

## 12) Отзывы

После `completed` у обеих сторон: оценка **1–5 звёзд** и необязательный текст. Видны в профиле / карточке исполнителя. Это не внутренний рейтинг.

## 13) Уведомления

Экран всех уведомлений у обеих ролей. Просмотр уведомления не меняет статус запроса.
